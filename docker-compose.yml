# LOCAL DEVELOPMENT ONLY
# Production uses: Upstash Redis (configured via env vars)
# Run locally: docker compose up -d

version: '3.8'
services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    command: redis-server --maxmemory 256mb --maxmemory-policy allkeys-lru
